# Классификация в Machine Learning
## Теоретический материал

## Содержание
1. [Введение в классификацию](#введение)
2. [Типы классификации](#типы)
3. [Работа с категориальными признаками](#категориальные)
4. [Feature Engineering](#feature-engineering)
5. [Алгоритм KNN](#knn)
6. [Процесс обучения](#обучение)
7. [Корреляция](#корреляция)

---

## 1. Введение в классификацию {#введение}

**Классификация** - это задача машинного обучения с учителем, где модель предсказывает категорию (класс) объекта.

### Примеры задач классификации:
- Определение спама в email
- Распознавание рукописных цифр
- Диагностика заболеваний
- Определение тональности текста
- Распознавание лиц
- Классификация изображений

---

## 2. Типы классификации {#типы}

### 2.1 Бинарная классификация
Два класса: 0 или 1, True или False, Да или Нет

**Примеры:**
- Спам или не спам
- Болен или здоров
- Кредит одобрен или отклонен
- Мошенническая транзакция или нет

**Особенности:**
- Простая интерпретация результатов
- Легко оценить качество (accuracy, precision, recall)
- Часто используется в бизнес-задачах

### 2.2 Мультикласс классификация
Более двух классов, но объект принадлежит **только одному** классу

**Примеры:**
- Распознавание цифр (0-9)
- Классификация видов цветов (Iris dataset)
- Определение языка текста
- Классификация типа документа

**Особенности:**
- Требует больше данных для обучения
- Сложнее интерпретировать результаты
- Может использовать стратегии One-vs-Rest или One-vs-One

### 2.3 Мультилейбл классификация
Объект может принадлежать **нескольким классам одновременно**

**Примеры:**
- Теги для статей (спорт, политика, экономика)
- Жанры фильмов (комедия, драма, боевик)
- Медицинские диагнозы (может быть несколько заболеваний)
- Классификация изображений (на фото может быть кот И собака)

**Особенности:**
- Каждый класс рассматривается как отдельная бинарная задача
- Требует специальных метрик оценки
- Более сложная задача, чем мультикласс

---

## 3. Работа с категориальными признаками {#категориальные}

Категориальные признаки - это признаки, которые принимают ограниченное количество значений (категорий)

### Типы категориальных признаков:

#### 3.1 Номинальные признаки
Нет естественного порядка между категориями

**Примеры:**
- Цвет (красный, синий, зеленый)
- Город (Москва, Санкт-Петербург, Казань)
- Пол (мужской, женский)
- Тип транспорта (автобус, метро, такси)

**Кодирование:** One-Hot Encoding

#### 3.2 Порядковые признаки
Есть естественный порядок между категориями

**Примеры:**
- Размер одежды (S, M, L, XL)
- Уровень образования (школа, бакалавр, магистр, PhD)
- Оценки (отлично, хорошо, удовлетворительно)
- Приоритет задачи (низкий, средний, высокий)

**Кодирование:** Label Encoding или Ordinal Encoding

### 3.3 Label Encoding
Присвоение числовых меток категориям

```python
from sklearn.preprocessing import LabelEncoder

sizes = ['S', 'M', 'L', 'XL', 'M', 'S', 'L']
le = LabelEncoder()
sizes_encoded = le.fit_transform(sizes)
# Результат: [0, 1, 2, 3, 1, 0, 2]
```

**Преимущества:**
- Простота реализации
- Экономия памяти
- Подходит для порядковых признаков

**Недостатки:**
- Создает искусственный порядок для номинальных признаков
- Может ввести модель в заблуждение

### 3.4 One-Hot Encoding
Создание бинарных столбцов для каждой категории

```python
import pandas as pd

colors = pd.DataFrame({'color': ['red', 'blue', 'green', 'red', 'blue']})
colors_encoded = pd.get_dummies(colors, columns=['color'])
```

**Преимущества:**
- Не создает искусственного порядка
- Подходит для номинальных признаков
- Легко интерпретировать

**Недостатки:**
- Увеличивает размерность данных
- Может привести к проблеме "проклятия размерности"
- Требует больше памяти

---

## 4. Feature Engineering {#feature-engineering}

**Feature Engineering** - процесс создания новых признаков из существующих для улучшения качества модели

### 4.1 Основные техники:

#### Создание новых признаков
- Комбинирование существующих признаков
- Извлечение информации из дат (день недели, месяц, год)
- Создание агрегированных признаков (среднее, сумма, максимум)

**Пример:**
```python
# Создание нового признака из существующих
df['BMI'] = df['weight'] / (df['height'] ** 2)
df['age_group'] = pd.cut(df['age'], bins=[0, 18, 35, 60, 100])
```

#### Преобразование признаков
- Логарифмирование (для нормализации распределения)
- Степенные преобразования
- Биннинг (разбиение на интервалы)

**Пример:**
```python
# Логарифмирование для нормализации
df['log_income'] = np.log1p(df['income'])

# Биннинг
df['age_bins'] = pd.cut(df['age'], bins=5)
```

#### Агрегация признаков
- Группировка и вычисление статистик
- Создание признаков на основе временных окон
- Подсчет частоты появления

**Пример:**
```python
# Агрегация по группам
df['avg_price_by_category'] = df.groupby('category')['price'].transform('mean')
```

### 4.2 Feature Scaling (Масштабирование признаков)

Приведение признаков к одному масштабу - важно для многих алгоритмов (особенно KNN, SVM, нейронные сети)

#### StandardScaler (Z-score нормализация)
Приводит признаки к нулевому среднему и единичной дисперсии

```python
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)
```

**Формула:** `z = (x - μ) / σ`

**Когда использовать:**
- Когда признаки имеют нормальное распределение
- Для алгоритмов, чувствительных к масштабу (KNN, SVM, нейронные сети)

#### MinMaxScaler
Приводит признаки к диапазону [0, 1]

```python
from sklearn.preprocessing import MinMaxScaler

scaler = MinMaxScaler()
X_scaled = scaler.fit_transform(X)
```

**Формула:** `x_scaled = (x - x_min) / (x_max - x_min)`

**Когда использовать:**
- Когда нужен конкретный диапазон значений
- Когда распределение не нормальное

---

## 5. Алгоритм K-Nearest Neighbors (KNN) {#knn}

**KNN** - простой и интуитивный алгоритм классификации, основанный на расстоянии между объектами

### 5.1 Принцип работы:

1. **Выбор K** - количество ближайших соседей
2. **Вычисление расстояния** до всех объектов в обучающей выборке
3. **Поиск K ближайших соседей**
4. **Голосование** - выбор класса, который встречается чаще всего среди K соседей

### 5.2 Метрики расстояния:

#### Евклидово расстояние (по умолчанию)
```
d = √[(x₁-x₂)² + (y₁-y₂)²]
```

#### Манхэттенское расстояние
```
d = |x₁-x₂| + |y₁-y₂|
```

#### Расстояние Минковского
```
d = (Σ|xᵢ-yᵢ|ᵖ)^(1/p)
```

### 5.3 Выбор K:

**Маленькое K (например, K=1):**
- ✅ Модель более гибкая
- ❌ Чувствительна к шуму
- ❌ Переобучение

**Большое K (например, K=100):**
- ✅ Модель более стабильная
- ❌ Может упустить локальные паттерны
- ❌ Недообучение

**Оптимальное K:**
- Обычно выбирается через кросс-валидацию
- Часто используют нечетные значения для бинарной классификации
- Типичные значения: 3, 5, 7, 9

### 5.4 Преимущества KNN:

- ✅ Простота реализации и понимания
- ✅ Не требует обучения (lazy learning)
- ✅ Хорошо работает на небольших данных
- ✅ Может работать с нелинейными данными

### 5.5 Недостатки KNN:

- ❌ Медленные предсказания (нужно вычислять расстояние до всех точек)
- ❌ Чувствителен к масштабу признаков
- ❌ Плохо работает на больших данных
- ❌ Проблемы с высокой размерностью (curse of dimensionality)
- ❌ Требует много памяти

---

## 6. Процесс обучения {#обучение}

### 6.1 Основные концепции:

#### Функция потерь (Loss Function)
Измеряет ошибку модели

**Примеры:**
- Mean Squared Error (MSE) - для регрессии
- Cross-Entropy - для классификации
- Hinge Loss - для SVM

#### Оптимизация
Процесс минимизации функции потерь

**Методы:**
- Градиентный спуск
- Стохастический градиентный спуск (SGD)
- Adam, RMSprop, AdaGrad

### 6.2 Эпохи (Epochs)

**Эпоха** - это один полный проход по всем обучающим данным

**Зачем нужно несколько эпох?**
- Модель учится постепенно
- Каждая эпоха улучшает качество
- Слишком много эпох → переобучение
- Слишком мало эпох → недообучение

**Как выбрать количество эпох?**
- Мониторинг метрик на валидационной выборке
- Early stopping - остановка при ухудшении метрик
- Обычно 10-100 эпох для простых задач

### 6.3 Батчи (Batches)

**Батч** - подмножество данных, используемое для одного обновления весов

#### Типы градиентного спуска:

**1. Batch Gradient Descent**
- Использует все данные для одного обновления
- ✅ Стабильная сходимость
- ❌ Медленный на больших данных
- ❌ Требует много памяти

**2. Stochastic Gradient Descent (SGD)**
- Использует 1 объект для обновления
- ✅ Быстрый
- ✅ Может выйти из локальных минимумов
- ❌ Нестабильная сходимость

**3. Mini-batch Gradient Descent**
- Использует небольшую группу объектов (обычно 32, 64, 128, 256)
- ✅ Баланс между скоростью и стабильностью
- ✅ Эффективное использование памяти
- ✅ Наиболее популярный подход

### 6.4 Градиентный спуск

**Принцип:**
1. Вычисляем градиент функции потерь
2. Делаем шаг в направлении, противоположном градиенту
3. Повторяем до сходимости

**Формула обновления весов:**
```
w = w - α * ∇L(w)
```
где:
- w - веса модели
- α - learning rate (скорость обучения)
- ∇L(w) - градиент функции потерь

**Learning Rate (скорость обучения):**
- Слишком большой → модель не сходится
- Слишком маленький → медленное обучение
- Обычно 0.001 - 0.1

---

## 7. Корреляция {#корреляция}

**Корреляция** - статистическая взаимосвязь между признаками

### 7.1 Коэффициент корреляции Пирсона

Измеряет **линейную** зависимость между признаками

**Формула:**
```
r = Σ[(x - x̄)(y - ȳ)] / √[Σ(x - x̄)² * Σ(y - ȳ)²]
```

**Значения:**
- r = 1: идеальная положительная корреляция
- r = 0: нет линейной корреляции
- r = -1: идеальная отрицательная корреляция

**Интерпретация:**
- |r| < 0.3: слабая корреляция
- 0.3 ≤ |r| < 0.7: умеренная корреляция
- |r| ≥ 0.7: сильная корреляция

**Когда использовать:**
- Для непрерывных признаков
- Когда зависимость линейная
- Когда данные нормально распределены

### 7.2 Ранговая корреляция Спирмена

Измеряет **монотонную** зависимость (не обязательно линейную)

**Когда использовать:**
- Для порядковых признаков
- Когда зависимость нелинейная, но монотонная
- Когда есть выбросы
- Когда распределение не нормальное

**Преимущества:**
- Устойчива к выбросам
- Работает с нелинейными зависимостями
- Подходит для порядковых данных

### 7.3 Корреляция Кендалла

Измеряет согласованность порядка между признаками

**Когда использовать:**
- Для малых выборок
- Для порядковых данных
- Когда много одинаковых значений (ties)

### 7.4 Мультиколлинеарность

**Проблема:** Когда признаки сильно коррелируют друг с другом

**Последствия:**
- Нестабильность коэффициентов модели
- Сложность интерпретации
- Переобучение
- Увеличение дисперсии оценок

**Как обнаружить:**
- Корреляционная матрица (|r| > 0.7-0.8)
- VIF (Variance Inflation Factor) > 10

**Как бороться:**
- Удалить один из коррелирующих признаков
- PCA (Principal Component Analysis)
- Ridge/Lasso регрессия
- Feature selection

---

## 8. Практические рекомендации

### 8.1 Подготовка данных:
1. ✅ Всегда разделяйте данные на train/test
2. ✅ Используйте stratify для несбалансированных классов
3. ✅ Масштабируйте признаки для KNN и логистической регрессии
4. ✅ Проверяйте на пропущенные значения
5. ✅ Анализируйте распределение признаков

### 8.2 Выбор модели:
1. ✅ Начинайте с простых моделей (Logistic Regression, KNN)
2. ✅ Используйте кросс-валидацию для оценки
3. ✅ Сравнивайте несколько моделей
4. ✅ Учитывайте интерпретируемость
5. ✅ Проверяйте на переобучение

### 8.3 Оценка качества:
1. ✅ Используйте несколько метрик (accuracy, precision, recall, F1)
2. ✅ Анализируйте confusion matrix
3. ✅ Проверяйте на тестовой выборке
4. ✅ Используйте кросс-валидацию
5. ✅ Визуализируйте результаты

### 8.4 Feature Engineering:
1. ✅ Анализируйте корреляцию признаков
2. ✅ Создавайте новые признаки на основе доменных знаний
3. ✅ Удаляйте мультиколлинеарные признаки
4. ✅ Используйте feature selection
5. ✅ Экспериментируйте с преобразованиями

---

## 9. Полезные ресурсы

### Документация:
- [Scikit-learn Documentation](https://scikit-learn.org/)
- [Pandas Documentation](https://pandas.pydata.org/)
- [NumPy Documentation](https://numpy.org/)

### Обучающие материалы:
- [Machine Learning Crash Course (Google)](https://developers.google.com/machine-learning/crash-course)
- [Coursera: Machine Learning by Andrew Ng](https://www.coursera.org/learn/machine-learning)
- [Fast.ai](https://www.fast.ai/)

### Практика:
- [Kaggle](https://www.kaggle.com/) - соревнования и датасеты
- [UCI Machine Learning Repository](https://archive.ics.uci.edu/ml/)
- [Google Dataset Search](https://datasetsearch.research.google.com/)

### Книги:
- "Hands-On Machine Learning" by Aurélien Géron
- "Pattern Recognition and Machine Learning" by Christopher Bishop
- "The Elements of Statistical Learning" by Hastie, Tibshirani, Friedman

---

## 10. Заключение

Классификация - это фундаментальная задача машинного обучения, которая имеет множество практических применений. Понимание основных концепций, таких как типы классификации, работа с категориальными признаками, feature engineering и алгоритмы классификации, является ключом к успешному решению задач машинного обучения.

**Ключевые выводы:**
1. Выбирайте правильный тип классификации для вашей задачи
2. Правильно обрабатывайте категориальные признаки
3. Используйте feature engineering для улучшения качества модели
4. Масштабируйте признаки для алгоритмов, чувствительных к масштабу
5. Всегда оценивайте модель на отдельной тестовой выборке
6. Используйте кросс-валидацию для надежной оценки
7. Анализируйте корреляцию и боритесь с мультиколлинеарностью

**Следующие шаги:**
- Практикуйтесь на реальных датасетах
- Участвуйте в соревнованиях на Kaggle
- Изучайте более продвинутые алгоритмы (Random Forest, Gradient Boosting, Neural Networks)
- Углубляйтесь в feature engineering и feature selection
- Изучайте методы работы с несбалансированными данными

---

**Удачи в изучении машинного обучения! 🚀**
